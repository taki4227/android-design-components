<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:name="com.example.taki.android_design_components.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/navigate_to_animation"
            app:destination="@id/animation" />
    </fragment>

    <fragment
        android:id="@+id/animation"
        android:name="com.example.taki.android_design_components.AnimationFragment"
        android:label="Animation"
        tools:layout="@layout/fragment_animation" />

</navigation>